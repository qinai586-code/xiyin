# Portable local use: the current Windows account runs XIYIN.
# This explicitly selects application-level guards, not OS account isolation.
[identity]
mode = "single_user"

[runtime]
# Relative to this code root. The launcher uses this root, with no fixed drive/account.
allowed_cwd_roots = ["."]
